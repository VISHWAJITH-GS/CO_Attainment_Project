# Backend

This directory contains the Python scripts for the CO Attainment Project.

## Getting Started

1.  Make sure you are in the `backend` directory.
2.  Install the dependencies from `requirements.txt`:
    ```bash
    pip install -r requirements.txt
    ```
3. To run the scripts, execute them individually.
